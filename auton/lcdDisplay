#ifndef LCDDISPLAY_H
#define LCDDISPLAY_H

void lcdDisplay(){
/*
//LCD DISPLAY CODE
		bLCDBacklight = true;
		//clear top line
		clearLCDLine(0);
		//clear bottom line
		clearLCDLine(1);
		//set lcd positon to top right
		//set to bottem left
		displayLCDPos(1,0);
		//print
		displayNextLCDString("Pot: ");
		//displays potentiometer value
	//	displayNextLCDNumber(abs(nMotorEncoder(backRight)));
		//sets position
		displayLCDPos(0,6);
		//displays sensor value
		displayNextLCDNumber(SensorValue[autoSelect]);
	//	displayNextLCDNumber(abs(nMotorEncoder(backLeft)));
		*/
		while(true){
			autonomousSelector(0, 100, 1099, 2098, 3097);
		}
	}





#endif
