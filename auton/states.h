#ifndef STATES_H
#define STATES_H

enum moveDir {		//sets direction
	FORWARDS,
	RIGHT,
	BACKWARDS,
	LEFT
};

enum armDir {		//direction of arm
	UP,
	DOWN
};

enum clawPos {		//intake state
	CLOSED,
	OPEN
};

#endif
